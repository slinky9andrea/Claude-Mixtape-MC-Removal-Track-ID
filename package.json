{
  "name": "mixtape-mc-removal-track-id",
  "version": "1.0.0",
  "description": "Strip MC speech from DJ mixtapes and identify songs using LALAL.AI + AudD",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
